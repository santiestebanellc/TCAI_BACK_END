<form method="post" action="{{ path('app_dieta_has_tipo_dieta_delete', {'id': dieta_has_tipo_dietum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dieta_has_tipo_dietum.id) }}">
    <button class="btn">Delete</button>
</form>
