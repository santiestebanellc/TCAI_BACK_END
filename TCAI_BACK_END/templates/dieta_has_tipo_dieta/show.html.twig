{% extends 'base.html.twig' %}

{% block title %}DietaHasTipoDieta{% endblock %}

{% block body %}
    <h1>DietaHasTipoDieta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dieta_has_tipo_dietum.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dieta_has_tipo_dieta_index') }}">back to list</a>

    <a href="{{ path('app_dieta_has_tipo_dieta_edit', {'id': dieta_has_tipo_dietum.id}) }}">edit</a>

    {{ include('dieta_has_tipo_dieta/_delete_form.html.twig') }}
{% endblock %}
