<form method="post" action="{{ path('app_observacion_delete', {'id': observacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ observacion.id) }}">
    <button class="btn">Delete</button>
</form>
