{% extends 'base.html.twig' %}

{% block title %}Higiene{% endblock %}

{% block body %}
    <h1>Higiene</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ higiene.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ higiene.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_higiene_index') }}">back to list</a>

    <a href="{{ path('app_higiene_edit', {'id': higiene.id}) }}">edit</a>

    {{ include('higiene/_delete_form.html.twig') }}
{% endblock %}
