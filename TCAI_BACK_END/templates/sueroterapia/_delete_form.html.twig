<form method="post" action="{{ path('app_sueroterapia_delete', {'id': sueroterapium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sueroterapium.id) }}">
    <button class="btn">Delete</button>
</form>
