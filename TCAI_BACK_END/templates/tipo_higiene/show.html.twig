{% extends 'base.html.twig' %}

{% block title %}TipoHigiene{% endblock %}

{% block body %}
    <h1>TipoHigiene</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_higiene.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tipo_higiene.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tipo_higiene_index') }}">back to list</a>

    <a href="{{ path('app_tipo_higiene_edit', {'id': tipo_higiene.id}) }}">edit</a>

    {{ include('tipo_higiene/_delete_form.html.twig') }}
{% endblock %}
