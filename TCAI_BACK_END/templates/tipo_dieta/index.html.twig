{% extends 'base.html.twig' %}

{% block title %}TipoDieta index{% endblock %}

{% block body %}
    <h1>TipoDieta index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tipo_dietum in tipo_dietas %}
            <tr>
                <td>{{ tipo_dietum.id }}</td>
                <td>{{ tipo_dietum.descripcion }}</td>
                <td>
                    <a href="{{ path('app_tipo_dieta_show', {'id': tipo_dietum.id}) }}">show</a>
                    <a href="{{ path('app_tipo_dieta_edit', {'id': tipo_dietum.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tipo_dieta_new') }}">Create new</a>
{% endblock %}
