{% extends 'base.html.twig' %}

{% block title %}TipoDieta{% endblock %}

{% block body %}
    <h1>TipoDieta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_dietum.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tipo_dietum.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tipo_dieta_index') }}">back to list</a>

    <a href="{{ path('app_tipo_dieta_edit', {'id': tipo_dietum.id}) }}">edit</a>

    {{ include('tipo_dieta/_delete_form.html.twig') }}
{% endblock %}
