{% extends 'base.html.twig' %}

{% block title %}Drenaje{% endblock %}

{% block body %}
    <h1>Drenaje</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ drenaje.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ drenaje.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_drenaje_index') }}">back to list</a>

    <a href="{{ path('app_drenaje_edit', {'id': drenaje.id}) }}">edit</a>

    {{ include('drenaje/_delete_form.html.twig') }}
{% endblock %}
