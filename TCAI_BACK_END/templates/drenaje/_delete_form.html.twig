<form method="post" action="{{ path('app_drenaje_delete', {'id': drenaje.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ drenaje.id) }}">
    <button class="btn">Delete</button>
</form>
