<form method="post" action="{{ path('app_auxiliar_delete', {'id': auxiliar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auxiliar.id) }}">
    <button class="btn">Delete</button>
</form>
