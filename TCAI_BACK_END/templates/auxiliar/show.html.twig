{% extends 'base.html.twig' %}

{% block title %}Auxiliar{% endblock %}

{% block body %}
    <h1>Auxiliar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auxiliar.id }}</td>
            </tr>
            <tr>
                <th>Num_trabajador</th>
                <td>{{ auxiliar.numTrabajador }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ auxiliar.nombre }}</td>
            </tr>
            <tr>
                <th>Apellidos</th>
                <td>{{ auxiliar.apellidos }}</td>
            </tr>
            <tr>
                <th>Contraseña</th>
                <td>{{ auxiliar.contraseña }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_auxiliar_index') }}">back to list</a>

    <a href="{{ path('app_auxiliar_edit', {'id': auxiliar.id}) }}">edit</a>

    {{ include('auxiliar/_delete_form.html.twig') }}
{% endblock %}
