<form method="post" action="{{ path('app_balance_hidrico_delete', {'id': balance_hidrico.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ balance_hidrico.id) }}">
    <button class="btn">Delete</button>
</form>
